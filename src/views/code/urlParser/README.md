# URL 编解码工具

在线 URL 编码和解码工具，支持多种编码方式，提供常用示例和详细的编码规则说明。

## 📋 功能特性

### 核心功能

- ✅ **URL 编码** - 将文本编码为 URL 安全格式
- ✅ **URL 解码** - 将编码的 URL 解码回原始文本
- ✅ **多种编码方式** - 支持 encodeURIComponent、encodeURI、escape
- ✅ **实时字符统计** - 显示输入输出的字符数量
- ✅ **一键复制** - 快速复制编码/解码结果

### 辅助功能

- ✅ **常用示例** - 6+ 常见 URL 编码场景
- ✅ **编码规则说明** - 20+ 常用字符的编码规则
- ✅ **保留字符说明** - URL 保留字符及其含义
- ✅ **快速应用** - 点击示例直接应用到编辑器

## 🎨 界面布局

```
┌─────────────────────────────────────────────────────────┐
│  工具栏: [URL 编码] [URL 解码] [复制结果] ... [清空]    │
├─────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐             │
│  │ 📝 输入          │  │ ✅ 输出          │             │
│  │  (textarea)      │  │  (textarea)      │             │
│  │  xxx 字符        │  │  xxx 字符        │             │
│  └──────────────────┘  └──────────────────┘             │
├─────────────────────────────────────────────────────────┤
│  ⚙️ 编码选项                                             │
│  ○ encodeURIComponent  ○ encodeURI  ○ escape           │
├─────────────────────────────────────────────────────────┤
│  📚 常用示例（6+ 示例）                                   │
│  - 中文参数  - 特殊字符  - 邮箱地址等                     │
├─────────────────────────────────────────────────────────┤
│  📖 编码说明（20+ 规则）                                  │
│  - 空格 → %20  - # → %23  - & → %26 等                 │
├─────────────────────────────────────────────────────────┤
│  🔐 保留字符（18+ 字符）                                  │
│  : / ? # [ ] @ ! $ & ' ( ) * + , ; =                  │
└─────────────────────────────────────────────────────────┘
```

## 🔧 使用方法

### 基础编码

1. **输入文本**
   ```
   在输入框中输入需要编码的文本：
   https://example.com?name=张三&age=20
   ```

2. **选择编码类型**（可选）
   - `encodeURIComponent` - 编码所有字符（推荐）
   - `encodeURI` - 保留 URL 结构字符
   - `escape` - 旧版编码（不推荐）

3. **点击 "URL 编码" 按钮**
   ```
   结果：
   https://example.com?name=%E5%BC%A0%E4%B8%89&age=20
   ```

4. **复制结果**
   - 点击 "复制结果" 按钮复制到剪贴板

### 基础解码

1. **输入编码后的文本**
   ```
   https://example.com?name=%E5%BC%A0%E4%B8%89&age=20
   ```

2. **点击 "URL 解码" 按钮**
   ```
   结果：
   https://example.com?name=张三&age=20
   ```

### 使用示例

点击任意示例卡片，自动填充输入和输出区域，方便学习和参考。

## 📚 编码方式对比

### encodeURIComponent（推荐）✅

**用途**：编码 URL 参数值

**特点**：
- 编码所有特殊字符
- 适用于查询字符串的参数值
- 最常用，最安全

**示例**：
```javascript
encodeURIComponent('张三')
// 结果: %E5%BC%A0%E4%B8%89

encodeURIComponent('hello world')
// 结果: hello%20world

encodeURIComponent('user@example.com')
// 结果: user%40example.com
```

**不编码的字符**：
- 字母数字：`A-Z a-z 0-9`
- 未保留标记：`- _ . ! ~ * ' ( )`

### encodeURI

**用途**：编码完整的 URI

**特点**：
- 保留 URL 结构字符
- 适用于编码完整的 URL
- 不会编码 URL 中有特殊含义的字符

**示例**：
```javascript
encodeURI('https://example.com/张三/profile')
// 结果: https://example.com/%E5%BC%A0%E4%B8%89/profile

encodeURI('https://example.com?name=张三&age=20')
// 结果: https://example.com?name=%E5%BC%A0%E4%B8%89&age=20
```

**不编码的字符**：
- URL 结构字符：`: / ? # [ ] @ ! $ & ' ( ) * + , ; =`
- 未保留标记：`- _ . ! ~ * ' ( )`

### escape（已废弃）⚠️

**用途**：旧版 JavaScript 编码方式

**特点**：
- 已被废弃，不推荐使用
- 使用十六进制转义序列
- 不遵循 UTF-8 编码规范

**示例**：
```javascript
escape('张三')
// 结果: %u5F20%u4E09（非标准格式）

escape('hello world')
// 结果: hello%20world
```

## 💡 使用场景

### 场景 1：GET 请求参数

**需求**：发送带中文参数的 GET 请求

**解决方案**：
```javascript
const name = '张三';
const age = 20;

// 使用 encodeURIComponent 编码参数值
const url = `https://api.example.com/user?name=${encodeURIComponent(name)}&age=${age}`;

// 结果: https://api.example.com/user?name=%E5%BC%A0%E4%B8%89&age=20
```

### 场景 2：动态 URL 生成

**需求**：根据用户输入生成 URL

**解决方案**：
```javascript
const search = '你好 世界';

// 编码搜索关键词
const url = `https://www.google.com/search?q=${encodeURIComponent(search)}`;

// 结果: https://www.google.com/search?q=%E4%BD%A0%E5%A5%BD%20%E4%B8%96%E7%95%8C
```

### 场景 3：URL 解析

**需求**：从 URL 中提取参数值

**解决方案**：
```javascript
const url = 'https://example.com?name=%E5%BC%A0%E4%B8%89';

// 提取并解码参数
const params = new URLSearchParams(url.split('?')[1]);
const name = decodeURIComponent(params.get('name') || '');

// 结果: 张三
```

### 场景 4：文件上传

**需求**：上传带中文名称的文件

**解决方案**：
```javascript
const fileName = '报告 2024.pdf';

// 编码文件名
const encodedName = encodeURIComponent(fileName);

// 用于 Content-Disposition 等 HTTP 头
// filename*=UTF-8''%E6%8A%A5%E5%91%8A%202024.pdf
```

### 场景 5：URL 跳转

**需求**：带参数跳转到其他页面

**解决方案**：
```javascript
const returnUrl = 'https://example.com/user/张三/profile';

// 编码回调地址
const loginUrl = `https://login.example.com?return=${encodeURIComponent(returnUrl)}`;

// 结果: https://login.example.com?return=https%3A%2F%2Fexample.com%2Fuser%2F%E5%BC%A0%E4%B8%89%2Fprofile
```

## 📖 编码规则说明

### 常用字符编码表

| 字符 | 说明 | 编码后 | 示例 |
|------|------|--------|------|
| 空格 | 空格符 | `%20` 或 `+` | `hello world` → `hello%20world` |
| `!` | 感叹号 | `%21` | `hello!` → `hello%21` |
| `#` | 井号 | `%23` | `#tag` → `%23tag` |
| `$` | 美元符号 | `%24` | `$100` → `%24100` |
| `%` | 百分号 | `%25` | `50%` → `50%25` |
| `&` | 与符号 | `%26` | `name&age` → `name%26age` |
| `'` | 单引号 | `%27` | `it's` → `it%27s` |
| `(` | 左括号 | `%28` | `(test)` → `%28test%29` |
| `)` | 右括号 | `%29` | |
| `*` | 星号 | `%2A` | `2*3` → `2%2A3` |
| `+` | 加号 | `%2B` | `1+1` → `1%2B1` |
| `,` | 逗号 | `%2C` | `a,b,c` → `a%2Cb%2Cc` |
| `/` | 斜杠 | `%2F` | `a/b` → `a%2Fb` |
| `:` | 冒号 | `%3A` | `key:value` → `key%3Avalue` |
| `;` | 分号 | `%3B` | `a;b` → `a%3Bb` |
| `=` | 等号 | `%3D` | `x=1` → `x%3D1` |
| `?` | 问号 | `%3F` | `what?` → `what%3F` |
| `@` | @符号 | `%40` | `user@domain` → `user%40domain` |
| `[` | 左方括号 | `%5B` | `[1,2]` → `%5B1,2%5D` |
| `]` | 右方括号 | `%5D` | |

### URL 保留字符

以下字符在 URL 中有特殊含义：

| 字符 | 含义 | 示例 |
|------|------|------|
| `:` | 协议分隔符 | `http://` |
| `/` | 路径分隔符 | `/user/profile` |
| `?` | 查询字符串开始 | `?name=value` |
| `#` | 锚点/片段 | `#section` |
| `@` | 用户信息 | `user:pass@host` |
| `&` | 参数分隔符 | `name=a&age=1` |
| `=` | 参数赋值 | `key=value` |

### 非 ASCII 字符编码

中文和其他非 ASCII 字符使用 UTF-8 编码：

```
张 → %E5%BC%A0
三 → %E4%B8%89
你好 → %E4%BD%A0%E5%A5%BD
```

每个中文字符通常编码为 3 个字节（3个 `%XX`）。

## 🎯 常见问题

### Q1: 空格编码成 `%20` 还是 `+`？

**答**：
- `%20` 是标准编码格式（推荐）
- `+` 是在 `application/x-www-form-urlencoded` 格式中的简写
- `encodeURIComponent` 使用 `%20`
- 表单提交时浏览器可能使用 `+`

### Q2: 什么时候用 `encodeURI`，什么时候用 `encodeURIComponent`？

**答**：
- **encodeURI**：编码完整的 URL
  ```javascript
  encodeURI('https://example.com/张三')
  ```
- **encodeURIComponent**：编码 URL 的一部分（参数值）
  ```javascript
  'https://example.com?name=' + encodeURIComponent('张三')
  ```

### Q3: 为什么解码失败？

**答**：可能的原因：
1. **编码格式不匹配**：用 `encodeURI` 编码的，要用 `decodeURI` 解码
2. **多次编码**：文本被编码了多次，需要多次解码
3. **格式错误**：`%` 后面不是有效的十六进制数

### Q4: 如何处理多次编码的情况？

**答**：循环解码直到结果不再变化
```javascript
function decodeMultiple(str) {
  let decoded = str;
  let prev;
  do {
    prev = decoded;
    try {
      decoded = decodeURIComponent(decoded);
    } catch {
      break;
    }
  } while (decoded !== prev);
  return decoded;
}
```

### Q5: `escape` 和 `encodeURIComponent` 有什么区别？

**答**：
- **escape**（已废弃）：
  - 使用 `%uXXXX` 格式编码 Unicode
  - 不符合 UTF-8 标准
  - 编码结果可能无法正确解析
- **encodeURIComponent**（推荐）：
  - 使用标准 UTF-8 编码
  - 符合 RFC 3986 规范
  - 现代浏览器都支持

## 💡 使用技巧

### 1. 批量处理

```javascript
// 编码多个参数
const params = {
  name: '张三',
  city: '北京',
  tags: ['前端', '开发']
};

const queryString = Object.entries(params)
  .map(([key, value]) => 
    `${encodeURIComponent(key)}=${encodeURIComponent(String(value))}`
  )
  .join('&');
```

### 2. 安全的 URL 构建

```javascript
// ✅ 安全的方式
const baseUrl = 'https://api.example.com/search';
const params = new URLSearchParams({
  q: '你好 世界',
  page: '1'
});
const url = `${baseUrl}?${params}`;

// ❌ 不安全的方式（可能导致 XSS）
const unsafeUrl = `https://api.example.com/search?q=${userInput}`;
```

### 3. 检测是否需要编码

```javascript
function needsEncoding(str) {
  return str !== encodeURIComponent(str);
}

console.log(needsEncoding('hello'));  // false
console.log(needsEncoding('你好'));   // true
console.log(needsEncoding('hello world'));  // true（有空格）
```

## ⚠️ 注意事项

1. **编码时机**
   - 在发送请求前编码
   - 不要对已编码的内容再次编码

2. **解码时机**
   - 接收到数据后立即解码
   - 后端通常会自动解码 URL 参数

3. **安全考虑**
   - 不要在前端存储敏感信息的编码版本
   - 编码不等于加密，仍然可以被解码

4. **兼容性**
   - `encodeURIComponent` 和 `decodeURIComponent` 所有现代浏览器都支持
   - `escape` 和 `unescape` 已废弃，避免使用

5. **字符集**
   - 始终使用 UTF-8 字符集
   - 确保服务器也使用 UTF-8 解码

## 📚 相关资源

- [MDN - encodeURIComponent](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/encodeURIComponent)
- [MDN - encodeURI](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/encodeURI)
- [RFC 3986 - URI 规范](https://tools.ietf.org/html/rfc3986)
- [URL 编码规范](https://en.wikipedia.org/wiki/Percent-encoding)

## 🎉 总结

URL 编解码工具提供了：

- ✅ 3 种编码方式（encodeURIComponent、encodeURI、escape）
- ✅ 6+ 常用示例
- ✅ 20+ 编码规则说明
- ✅ 18+ 保留字符说明
- ✅ 实时字符统计
- ✅ 一键复制功能

适用于：
- 🔗 URL 参数编码
- 📝 API 请求构建
- 🌐 Web 开发调试
- 📊 数据传输处理

---

**提示**：推荐使用 `encodeURIComponent` 进行 URL 参数编码，这是最安全和最常用的方式！
