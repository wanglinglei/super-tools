# 颜色转换工具

功能强大的在线颜色格式转换工具，支持 HEX、RGB、HSL、HSV 等多种格式的相互转换。

## 📁 文件结构

```
color/
├── index.vue                      # 主页面组件（UI + 交互）
├── constants.ts                   # 颜色格式配置常量 ⭐
├── transform.ts                   # 颜色转换函数库（核心算法）⭐
├── transform.md                   # 转换函数库 API 文档
├── components/
│   ├── ColorFormatCard.vue        # 颜色格式卡片组件 ⭐
│   └── README.md                  # 组件使用文档
└── README.md                      # 工具使用说明（本文件）
```

**核心模块说明**：

1. **`constants.ts`** - 配置常量：
   - 颜色格式配置 (`COLOR_FORMATS`)
   - 默认颜色值 (`DEFAULT_COLOR`)
   - 类型定义 (`ColorFormatType`, `ColorFormatConfig`)

2. **`transform.ts`** - 转换函数库：
   - 包含所有颜色转换算法
   - 完整的 JSDoc 注释
   - TypeScript 类型定义
   - 可独立导入使用
   - 详细使用文档请查看 `transform.md`

3. **`ColorFormatCard.vue`** - 格式卡片组件：
   - 可复用的颜色格式输入/输出卡片
   - 支持任意颜色格式
   - 详细使用文档请查看 `components/README.md`

## 功能特性

### 1. 颜色选择器
- ✅ 原生 HTML5 颜色选择器
- ✅ 实时预览选中的颜色
- ✅ 自动同步到所有格式输入框

### 2. 全局透明度控制
- ✅ 滑块调节透明度（0-1）
- ✅ 实时显示透明度数值
- ✅ 影响所有 RGBA/HSLA 格式输出
- ✅ 可视化预览透明度效果

### 3. 多格式输入支持

#### HEX 输入
- 支持格式：`#43ad7f`、`#FFFFFF`
- 自动转换为：RGB、HSL、HSV
- 实时颜色预览

#### RGB 输入
- 支持格式：`rgb(67, 173, 127)`、`rgba(67, 173, 127, 0.8)`
- 自动转换为：HEX、HSL、HSV
- 实时颜色预览

#### HSL 输入
- 支持格式：`hsl(154, 44.2%, 47.1%)`、`hsla(154, 44.2%, 47.1%, 0.8)`
- 自动转换为：HEX、RGB、HSV
- 实时颜色预览

#### HSV 输入
- 支持格式：`hsv(154, 61.3%, 67.8%)`
- 自动转换为：HEX、RGB、HSL
- 实时颜色预览

### 4. 格式输出

每个输入框都会实时输出其他三种格式：
- **一键复制**：每个输出结果都带有复制图标按钮
- **实时更新**：输入改变时自动更新所有输出
- **高精度**：保留小数点，确保转换精确

## 使用说明

### 快速转换

1. **使用颜色选择器**
   - 点击顶部颜色选择器
   - 选择你想要的颜色
   - 所有格式会自动填充

2. **调整透明度**
   - 拖动透明度滑块
   - 查看预览区的透明效果
   - 带透明度的格式会自动更新（RGBA/HSLA）

3. **输入转换**
   - 在任意格式输入框中输入颜色值
   - 其他三种格式会自动转换
   - 点击复制图标按钮复制结果

### 输入格式示例

```css
/* HEX */
#43ad7f
#FF0000

/* RGB */
rgb(67, 173, 127)
rgba(67, 173, 127, 0.8)

/* HSL */
hsl(154, 44.2%, 47.1%)
hsla(154, 44.2%, 47.1%, 0.8)

/* HSV */
hsv(154, 61.3%, 67.8%)
```

## 技术实现

### 颜色空间转换算法

#### HEX ↔ RGB
```typescript
// HEX 转 RGB
function hexToRgb(hex: string): { r: number; g: number; b: number } | null {
  const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
  if (!result || !result[1] || !result[2] || !result[3]) return null;
  return {
    r: parseInt(result[1], 16),
    g: parseInt(result[2], 16),
    b: parseInt(result[3], 16),
  };
}

// RGB 转 HEX
function rgbToHex(r: number, g: number, b: number): string {
  return '#' + [r, g, b].map((x) => x.toString(16).padStart(2, '0')).join('');
}
```

#### RGB ↔ HSL
```typescript
// RGB 转 HSL
function rgbToHsl(r: number, g: number, b: number) {
  r /= 255;
  g /= 255;
  b /= 255;
  
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  let h = 0, s = 0;
  const l = (max + min) / 2;
  
  if (max !== min) {
    const d = max - min;
    s = l > 0.5 ? d / (2 - max - min) : d / (max + min);
    
    switch (max) {
      case r: h = ((g - b) / d + (g < b ? 6 : 0)) / 6; break;
      case g: h = ((b - r) / d + 2) / 6; break;
      case b: h = ((r - g) / d + 4) / 6; break;
    }
  }
  
  return {
    h: Math.round(h * 360),
    s: Math.round(s * 100 * 10) / 10,
    l: Math.round(l * 100 * 10) / 10,
  };
}
```

#### RGB ↔ HSV
```typescript
// RGB 转 HSV
function rgbToHsv(r: number, g: number, b: number) {
  r /= 255;
  g /= 255;
  b /= 255;
  
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  let h = 0;
  const v = max;
  const d = max - min;
  const s = max === 0 ? 0 : d / max;
  
  if (max !== min) {
    switch (max) {
      case r: h = ((g - b) / d + (g < b ? 6 : 0)) / 6; break;
      case g: h = ((b - r) / d + 2) / 6; break;
      case b: h = ((r - g) / d + 4) / 6; break;
    }
  }
  
  return {
    h: Math.round(h * 360),
    s: Math.round(s * 100 * 10) / 10,
    v: Math.round(v * 100 * 10) / 10,
  };
}
```

### 配置化架构 ⭐

使用配置化方式管理颜色格式，提高可维护性和可扩展性：

```typescript
// constants.ts
export const COLOR_FORMATS: ColorFormatConfig[] = [
  {
    type: 'hex',
    title: 'HEX 输入',
    format: 'HEX',
    placeholder: '#43ad7f',
    outputFormats: ['RGB', 'HSL', 'HSV'],
  },
  // ... 其他格式配置
];
```

**优势**：
- ✅ **易于维护**：所有配置集中管理
- ✅ **易于扩展**：添加新格式只需增加配置项
- ✅ **类型安全**：完整的 TypeScript 类型定义
- ✅ **代码简洁**：使用 `v-for` 循环渲染组件

### 组件化设计

使用 `ColorFormatCard` 组件封装格式卡片：

```vue
<!-- 使用 v-for 动态渲染 -->
<ColorFormatCard
  v-for="formatConfig in COLOR_FORMATS"
  :key="formatConfig.type"
  v-model="colorInputs[formatConfig.type]"
  :title="formatConfig.title"
  :format="formatConfig.format"
  :placeholder="formatConfig.placeholder"
  :preview-color="getPreviewColor(formatConfig.type)"
  :outputs="getOutputsArray(formatConfig.type)"
  @update:model-value="handleFormatInput(formatConfig.type)"
  @copy="copyText"
/>
```

**优势**：
- ✅ **DRY 原则**：消除重复代码
- ✅ **统一样式**：所有卡片样式一致
- ✅ **统一逻辑**：统一的处理函数
- ✅ **易于测试**：组件可独立测试

### 实时同步机制

统一的输入处理函数 `handleFormatInput()`：

```typescript
// 1. 解析输入为 RGB
function handleFormatInput(formatType: ColorFormatType) {
  let rgb: RgbColor | null = null;
  
  switch (formatType) {
    case 'hex': rgb = hexToRgb(colorInputs.hex); break;
    case 'rgb': rgb = parseRgb(colorInputs.rgb); break;
    case 'hsl': /* ... */ break;
    case 'hsv': /* ... */ break;
  }
  
  if (rgb) syncAllFormats(rgb);
}

// 2. 同步所有格式
function syncAllFormats(rgb: RgbColor) {
  const hex = rgbToHex(rgb.r, rgb.g, rgb.b);
  const hsl = rgbToHsl(rgb.r, rgb.g, rgb.b);
  const hsv = rgbToHsv(rgb.r, rgb.g, rgb.b);
  
  // 更新所有输入框
  colorInputs.hex = hex;
  colorInputs.rgb = `rgb(${rgb.r}, ${rgb.g}, ${rgb.b})`;
  colorInputs.hsl = `hsl(${hsl.h}, ${hsl.s}%, ${hsl.l}%)`;
  colorInputs.hsv = `hsv(${hsv.h}, ${hsv.s}%, ${hsv.v}%)`;
}
```

### 透明度处理

透明度滑块会影响支持 alpha 通道的格式：
- RGB → RGBA（当 alpha < 1）
- HSL → HSLA（当 alpha < 1）
- HEX 格式不支持透明度（需要使用 RGBA）

```typescript
// 动态添加透明度
const rgbOutput = alpha.value < 1 
  ? `rgba(${rgb.r}, ${rgb.g}, ${rgb.b}, ${alpha.value})`
  : `rgb(${rgb.r}, ${rgb.g}, ${rgb.b})`;
```

## UI 设计

### 布局结构
```
┌──────────────────────────────────────┐
│  🎨 颜色转换工具              [重置]  │
├──────────────────────────────────────┤
│  🎨 颜色选择                          │
│  [颜色选择器] [预览条]                │
│  全局透明度: ━━━━━━━━━━━━━━━ 1.0      │
├──────────────────────────────────────┤
│  ┌────────┐  ┌────────┐              │
│  │HEX输入 │  │RGB输入 │              │
│  │ 输出   │  │ 输出   │              │
│  └────────┘  └────────┘              │
│  ┌────────┐  ┌────────┐              │
│  │HSL输入 │  │HSV输入 │              │
│  │ 输出   │  │ 输出   │              │
│  └────────┘  └────────┘              │
└──────────────────────────────────────┘
```

### 视觉特点
- **卡片式设计**：每个格式独立卡片，清晰分区
- **实时预览**：每个输入框旁都有颜色预览块
- **一键复制**：输出结果右侧的复制图标按钮
- **响应式布局**：自动适配桌面和移动端

## 使用场景

1. **前端开发** - CSS 颜色值快速转换
2. **UI 设计** - 设计稿颜色格式转换
3. **品牌设计** - 品牌色多格式输出
4. **教学演示** - 颜色空间转换原理展示
5. **代码调试** - 快速验证颜色值

## 颜色格式说明

### HEX (十六进制)
- 格式：`#RRGGBB`
- 范围：`#000000` ~ `#FFFFFF`
- 优点：简洁，CSS 常用
- 缺点：不直观，不支持透明度（需 8 位格式）

### RGB (红绿蓝)
- 格式：`rgb(r, g, b)` 或 `rgba(r, g, b, a)`
- 范围：r/g/b: 0~255, a: 0~1
- 优点：直观，支持透明度
- 缺点：数值较长

### HSL (色相/饱和度/亮度)
- 格式：`hsl(h, s%, l%)` 或 `hsla(h, s%, l%, a)`
- 范围：h: 0~360°, s/l: 0~100%, a: 0~1
- 优点：符合人类对颜色的感知，易于调整
- 适用：主题色调整、颜色过渡

### HSV (色相/饱和度/明度)
- 格式：`hsv(h, s%, v%)`
- 范围：h: 0~360°, s/v: 0~100%
- 优点：符合色彩选择器的工作原理
- 适用：设计工具、图形软件

## 注意事项

1. **输入格式**：确保输入格式正确，否则无法转换
2. **透明度**：HEX 格式不支持透明度，需使用 RGBA/HSLA
3. **精度**：转换过程中可能有微小精度损失
4. **浏览器兼容**：颜色选择器在部分旧浏览器中可能样式不同

## 快捷键

- 无快捷键，通过鼠标/触摸操作

## 浏览器兼容性

- Chrome >= 90
- Firefox >= 88
- Safari >= 14
- Edge >= 90

## 未来优化

- [ ] 支持颜色库收藏
- [ ] 支持历史记录
- [ ] 支持渐变色生成
- [ ] 支持色板导入/导出
- [ ] 支持无障碍色彩对比度检测
- [ ] 支持更多颜色空间（LAB、CMYK）
- [ ] 支持批量转换
- [ ] 支持颜色混合计算

## 相关资源

- [MDN - CSS 颜色](https://developer.mozilla.org/zh-CN/docs/Web/CSS/color_value)
- [颜色空间转换算法](https://en.wikipedia.org/wiki/HSL_and_HSV)
- [RGB vs HSL vs HSV](https://www.nixsensor.com/blog/rgb-vs-hsl-vs-hsv/)
